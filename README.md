# 🚀 Pull Request Reviewer Assignment Service

Сервис для автоматического назначения ревьюеров на Pull Request'ы с поддержкой команд, перераспределения ревьюеров и статистики.

## 📋 Содержание

- [Описание](#-описание)
- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Архитектура](#-архитектура)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [API Endpoints](#-api-endpoints)
- [Тестирование](#-тестирование)
- [Docker](#-docker)
- [Структура проекта](#-структура-проекта)

## 🎯 Описание

Сервис автоматически назначает до двух активных ревьюеров из команды автора Pull Request'а при его создании. Поддерживает перераспределение ревьюеров, получение статистики и управление командами и пользователями.

### Основные функции:

- ✅ Автоматическое назначение до 2 ревьюеров из команды автора
- ✅ Перераспределение ревьюеров (только из той же команды)
- ✅ Идемпотентное слияние PR (можно мержить несколько раз)
- ✅ Неизменяемость списка ревьюеров после мерджа
- ✅ Статистика по ревьюерам и командам
- ✅ Управление активностью пользователей

## ✨ Возможности

- **Автоматическое назначение ревьюеров**: При создании PR автоматически выбираются до 2 активных ревьюеров из команды автора (исключая самого автора)
- **Перераспределение**: Замена ревьюера на случайного активного участника из той же команды
- **Идемпотентность**: Операция мерджа безопасна при повторных вызовах
- **Валидация данных**: Полная валидация входных данных на уровне handlers и моделей
- **Структурированное логирование**: Использование zap для эффективного логирования
- **Тестирование**: Интеграционные и E2E тесты

## 🛠 Технологии

- **Go 1.25** - Язык программирования
- **PostgreSQL 15** - База данных
- **Gin** - HTTP веб-фреймворк
- **Zap** - Структурированное логирование
- **Goose** - Миграции базы данных
- **Docker & Docker Compose** - Контейнеризация
- **pgx** - PostgreSQL драйвер

## 🏗 Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

```
┌─────────────────┐
│   HTTP Layer    │  Handlers (валидация, HTTP)
├─────────────────┤
│  Service Layer  │  Бизнес-логика
├─────────────────┤
│  Storage Layer  │  Работа с БД
└─────────────────┘
```

### Слои:

- **Handlers** (`internal/http/handlers/`) - HTTP обработчики, валидация запросов
- **Services** (`internal/service/`) - Бизнес-логика, интерфейсы сервисов
- **Storage** (`internal/storage/postgres/`) - Работа с базой данных
- **Models** (`internal/models/`) - Модели данных
- **Config** (`internal/config/`) - Конфигурация приложения
- **Logger** (`internal/logger/`) - Логирование

## 📦 Требования

- Go 1.25 или выше
- PostgreSQL 15 или выше
- Docker и Docker Compose (опционально)
- Make (опционально, для удобных команд)

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd avito-autumn-2025
```

### 2. Установка зависимостей

```bash
go mod download
```

или через Make:

```bash
make deps
```

## ⚙️ Конфигурация

Конфигурация задаётся исключительно переменными окружения. Для удобства в репозитории есть файл `env.example`. Скопируйте его и при необходимости измените значения:

```bash
cp env.example .env
```

Пример содержимого `.env`:

```
HOST=0.0.0.0
PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase
DB_SSLMODE=disable
DATABASE_URL=
TEST_DB_NAME=test_mydatabase
```

### Переменные окружения

Все параметры можно переопределить через переменные окружения (либо через `.env`, либо напрямую):

- `HOST` - Хост сервера (по умолчанию: `0.0.0.0`)
- `PORT` - Порт сервера (по умолчанию: `8080`)
- `DATABASE_URL` - Полный URL подключения к БД (приоритет над отдельными параметрами)
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `DB_SSLMODE` - Параметры БД
- `TEST_DB_NAME` - Имя тестовой БД

## 🚀 Запуск

### Локальный запуск

1. **Запустите PostgreSQL** (если еще не запущен):

```bash
docker run -d \
  --name postgres_db \
  -e POSTGRES_USER=myuser \
  -e POSTGRES_PASSWORD=mypassword \
  -e POSTGRES_DB=mydatabase \
  -p 5432:5432 \
  postgres:15
```

2. **Примените миграции**:

Миграции применяются автоматически при запуске приложения.

3. **Запустите приложение**:

```bash
go run ./cmd/main.go
```

или через Make:

```bash
make run
```

Приложение будет доступно по адресу: `http://localhost:8181`

### Сборка

```bash
make build
```

Бинарный файл будет создан в `bin/reviewer-service`

## 🐳 Docker

### Запуск через Docker Compose

Самый простой способ запустить весь стек:

```bash
make docker-run
```

или

```bash
docker-compose up -d
```

Это запустит:
- Приложение на порту `8080`
- PostgreSQL на порту `5432`

### Остановка

```bash
make docker-stop
```

или

```bash
docker-compose down
```

### Очистка

```bash
make docker-clean
```

## 📡 API Endpoints

Базовый URL: `http://localhost:8181/api/v1`

### Пользователи

#### Создать пользователя
```http
POST /api/v1/users
Content-Type: application/json

{
  "id": "user1",
  "username": "john_doe",
  "is_active": true
}
```

**Ответ:** `201 Created`
```json
{
  "id": "user1",
  "username": "john_doe",
  "is_active": true,
  "team_name": ""
}
```

#### Получить пользователя по ID
```http
GET /api/v1/users/:id
```

**Ответ:** `200 OK`
```json
{
  "id": "user1",
  "username": "john_doe",
  "is_active": true,
  "team_name": "backend"
}
```

### Команды

#### Создать команду
```http
POST /api/v1/team/add
Content-Type: application/json

{
  "team_name": "backend",
  "members": [
    {
      "id": "user1",
      "username": "john_doe",
      "is_active": true
    },
    {
      "id": "user2",
      "username": "jane_smith",
      "is_active": true
    }
  ]
}
```

#### Получить команду с участниками
```http
GET /api/v1/team/:teamName
```

### Pull Requests

#### Создать Pull Request
```http
POST /api/v1/pull-request/create
Content-Type: application/json

{
  "pull_request_id": "pr-123",
  "pull_request_name": "Add new feature",
  "author_id": "user1"
}
```

**Ответ:** `201 Created`
```json
{
  "pull_request_id": "pr-123",
  "pull_request_name": "Add new feature",
  "author_id": "user1",
  "status": "OPEN",
  "assigned_reviewers": ["user2", "user3"],
  "createdAt": "2025-11-16T10:00:00Z"
}
```

#### Слить Pull Request
```http
POST /api/v1/pull-request/merge
Content-Type: application/json

{
  "pull_request_id": "pr-123"
}
```

**Ответ:** `200 OK`
```json
{
  "message": "Pull request merged successfully"
}
```

#### Перераспределить ревьюера
```http
POST /api/v1/pull-request/reassign
Content-Type: application/json

{
  "pull_request_id": "pr-123",
  "old_user_id": "user2"
}
```

**Ответ:** `200 OK`
```json
{
  "message": "Reviewer reassigned successfully"
}
```

#### Получить PR по ревьюеру
```http
GET /api/v1/users/get-review?user_id=user2
```

или

```http
GET /api/v1/users/get-review?reviewer_id=user2
```

**Ответ:** `200 OK`
```json
[
  {
    "pull_request_id": "pr-123",
    "pull_request_name": "Add new feature",
    "author_id": "user1",
    "status": "OPEN"
  }
]
```

### Статистика

#### Получить статистику
```http
GET /api/v1/statistics
```

**Ответ:** `200 OK`
```json
{
  "generated_at": "2025-11-16T10:00:00Z",
  "total_prs": 10,
  "open_prs": 5,
  "merged_prs": 5,
  "reviewer_stats": [
    {
      "reviewer_id": "user2",
      "reviewer_name": "jane_smith",
      "assigned_prs_count": 8,
      "last_assigned_at": "2025-11-16T09:00:00Z"
    }
  ],
  "team_stats": [
    {
      "team_name": "backend",
      "member_count": 5,
      "active_member_count": 4,
      "prs_created": 10
    }
  ]
}
```

## 🧪 Тестирование

### Запуск всех тестов

```bash
make test
```

### Интеграционные тесты

```bash
make test-integration
```

### E2E тесты

```bash
make test-e2e
```

### Все тесты (интеграция + E2E)

```bash
make test-all
```

### Требования для тестов

Тесты автоматически создают тестовую базу данных `test_mydatabase` при необходимости. Убедитесь, что PostgreSQL запущен и доступен.

## 📁 Структура проекта

```
avito-autumn-2025/
├── cmd/
│   └── main.go                 # Точка входа приложения
├── internal/
│   ├── config/                 # Конфигурация
│   ├── http/                   # HTTP слой
│   │   ├── handlers/           # HTTP обработчики
│   │   └── server/             # HTTP сервер
│   ├── logger/                 # Логирование
│   ├── models/                 # Модели данных
│   ├── postgres/               # Подключение к БД и миграции
│   ├── service/                # Бизнес-логика
│   │   ├── pull_request/
│   │   ├── team/
│   │   └── user/
│   └── storage/                # Слой работы с БД
│       └── postgres/
├── migrations/                 # SQL миграции
├── tests/                      # Тесты
│   ├── e2e/                    # E2E тесты
│   └── integration/            # Интеграционные тесты
├── env.example                 # Пример переменных окружения
├── docker-compose.yml          # Docker Compose конфигурация
├── Dockerfile                  # Docker образ
├── Makefile                    # Make команды
├── go.mod                      # Go модули
└── README.md                   # Документация
```

## 🛠 Make команды

```bash
make help              # Показать все доступные команды
make build             # Собрать приложение
make run               # Запустить приложение локально
make test              # Запустить все тесты
make test-integration  # Запустить интеграционные тесты
make test-e2e          # Запустить E2E тесты
make test-all          # Запустить все тесты (интеграция + E2E)
make clean             # Очистить артефакты сборки
make docker-build      # Собрать Docker образ
make docker-run        # Запустить через Docker Compose
make docker-stop       # Остановить Docker контейнеры
make docker-clean      # Очистить Docker ресурсы
make fmt               # Отформатировать код
make deps              # Установить зависимости
```

## 🔒 Валидация данных

Все входные данные валидируются:

- **Обязательные поля** проверяются через теги `binding:"required"`
- **Дополнительная валидация** в handlers для критичных полей
- **Детальные сообщения об ошибках** с описанием проблемы

## 📊 Логирование

Проект использует структурированное логирование через `zap`:

- Уровни: `DEBUG`, `INFO`, `WARN`, `ERROR`
- Структурированные поля для контекста
- JSON формат для production

## 🗄 База данных

### Миграции

Миграции применяются автоматически при запуске приложения через `goose`.

### Схема БД

- `users` - Пользователи
- `teams` - Команды
- `pull_requests` - Pull Request'ы
- `pull_request_reviewers` - Связь PR и ревьюеров

## 📝 Примеры использования

### Создание пользователя и команды

```bash
# Создать пользователя
curl -X POST http://localhost:8181/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{
    "id": "user1",
    "username": "john_doe",
    "is_active": true
  }'

# Создать команду
curl -X POST http://localhost:8181/api/v1/team/add \
  -H "Content-Type: application/json" \
  -d '{
    "team_name": "backend",
    "members": [
      {"id": "user1", "username": "john_doe", "is_active": true},
      {"id": "user2", "username": "jane_smith", "is_active": true}
    ]
  }'
```

### Создание Pull Request

```bash
curl -X POST http://localhost:8181/api/v1/pull-request/create \
  -H "Content-Type: application/json" \
  -d '{
    "pull_request_id": "pr-123",
    "pull_request_name": "Add new feature",
    "author_id": "user1"
  }'
```

## 🤝 Разработка

### Форматирование кода

```bash
make fmt
```

### Установка зависимостей

```bash
make deps
```

## 📄 Лицензия

Этот проект создан в рамках тестового задания для Avito Autumn 2025.

## 👤 Автор

Разработано для Avito Autumn 2025 Internship Program

